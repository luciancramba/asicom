{
  "name": "issuedoc",
  "version": "0.1.0",
  "private": true,
  "description": "Issuedoc — AI document extraction → fișă de emitere for insurance issuance (ASICOM / Insuretech).",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "dev": "npm run dev -w web",
    "build": "npm run build -w @issuedoc/shared && npm run build -w web",
    "test": "npm run test --workspaces --if-present",
    "lint": "npm run lint --workspaces --if-present",
    "typecheck": "npm run typecheck --workspaces --if-present"
  }
}
