{
  "name": "asicom",
  "version": "0.1.0",
  "private": true,
  "description": "Asicom — AI document extraction → fișă de emitere for insurance issuance (ASICOM / Insuretech).",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "dev": "npm run dev -w web",
    "build": "npm run build -w @asicom/shared && npm run build -w web",
    "test": "npm run test --workspaces --if-present",
    "lint": "npm run lint --workspaces --if-present",
    "typecheck": "npm run typecheck --workspaces --if-present"
  }
}
